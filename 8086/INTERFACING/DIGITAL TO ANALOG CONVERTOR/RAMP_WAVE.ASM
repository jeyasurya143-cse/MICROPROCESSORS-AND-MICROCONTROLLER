//RAMP WAVEFORM

                OUTPUT 2500AD
.ORG 2000H
DATA SEGMENT
DATA ENDS
CODE SEGMENT
        ASSUME CS:CODE, DS:DATA
    MOV AX, DATA
    MOV DS, AX
    MOV AL, 80H
    MOV DX, FFE6H
    OUT DX, AL
L1:
    MOV AL, 00H
L2:
    MOV DX, FFE0H
    OUT DX, AL
    INC AL
    JNZ L2
    JMP L1
CODE ENDS
END
